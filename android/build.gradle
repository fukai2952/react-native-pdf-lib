
buildscript {
    repositories {
        jcenter()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:1.3.1'
    }
}

apply plugin: 'com.android.library'

android {
    compileSdkVersion 23
    buildToolsVersion '25.0.0'

    dexOptions {
        jumboMode true
    }

    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 22
        versionCode 1
        versionName "1.0"
        multiDexEnabled true

    }
    lintOptions {
        abortOnError false
    }
}

repositories {
    mavenCentral()
    flatDir {
        dirs 'libs'
    }
}

dependencies {
   compile 'com.tom_roush:pdfbox-android:1.8.9.1'
    compile 'com.facebook.react:react-native:+'
    compile "com.itextpdf:itextg:5.5.9"
    compile 'com.android.support:multidex:'
//    compile group: 'com.itextpdf', name: 'itext-asian', version: '5.2.0'
//    compile "com.madgag:scprov-jdk15on:1.47.0.2"
  //  compile "com.madgag:scpkix-jdk15on:1.47.0.2"
    //compile(name:'pdfbox-android-1.8.9.1', ext:'aar')
   // compile(name:'pdfbox-android-1.8.9.1', ext:'aar')
    //compile files('libs/pdfbox-android-1.8.9.1-sources.jar')
    //compile files('libs/pdfbox-android-1.8.9.1-sources.jar')
}
  